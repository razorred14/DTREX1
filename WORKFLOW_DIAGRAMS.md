# Configuration Workflow - Visual Guide

## Application Startup Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   User Opens Application                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
         ┌───────────────────────────────┐
         │  useAppConfiguration Hook     │
         │  (Runs automatically)         │
         └───────────────┬───────────────┘
                         │
           ┌─────────────┼─────────────┐
           │             │             │
           ▼             ▼             ▼
      ┌────────┐   ┌──────────┐   ┌──────────┐
      │ Check  │   │  Check   │   │ Determine│
      │Backend │   │ Chia Node│   │ Wallet   │
      │Health  │   │ Status   │   │ Ready    │
      │/health │   │/chia/... │   │Capability│
      └────┬───┘   └────┬─────┘   └────┬─────┘
           │            │              │
           ▼            ▼              ▼
      ┌─────────────────────────────────────┐
      │  ConfigurationStatus Component      │
      │  (Display to user if needed)        │
      └────────────────┬────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
   ┌─────────────┐            ┌──────────────┐
   │ All Ready?  │            │ Some Missing?│
   │   Hidden    │            │    Show      │
   └─────────────┘            │   Status UI  │
                              └──────────────┘
```

## Wallet Connection Decision Tree

```
User Clicks "Connect Wallet"
          │
          ▼
┌──────────────────────────────┐
│ WalletConnector Component    │
│ (checks config)              │
└──────────────┬───────────────┘
               │
               ▼
    ┌─────────────────────┐
    │ Is Backend Ready?   │
    │ (config.backendReady)
    └──┬─────────────┬────┘
      NO            YES
      │              │
      ▼              ▼
  ┌────────┐    ┌──────────────┐
  │ Show   │    │ Attempt      │
  │Error   │    │ Wallet Conn. │
  │Message │    │              │
  └────────┘    └────┬─────────┘
                     │
                ┌────┴─────┐
                │           │
                ▼           ▼
            Success      Failure
            ┌──┐      ┌──────────┐
            │✓ │      │ Show     │
            └──┘      │ Error    │
                      └──────────┘
```

## Status Panel Display Logic

```
ComponentMount
      │
      ▼
┌─────────────────────────────────────┐
│ Check Backend & Chia Node Status    │
└─────────┬───────────────────────────┘
          │
          ▼
   ┌─────────────────────┐
   │ Backend Ready?      │
   └──┬──────────────┬───┘
      NO            YES
      │              │
      ▼              ▼
  ┌───────┐    ┌──────────────────────┐
  │ Show  │    │ Chia Node Connected? │
  │Backend│    └──┬──────────────┬────┘
  │Error │      NO              YES
  │Panel │      │               │
  └───────┘      ▼               ▼
           ┌──────────┐    ┌──────────┐
           │Show Chia │    │Hidden    │
           │Warning   │    │All Good! │
           │Optional  │    │          │
           └──────────┘    └──────────┘
```

## User Experience Timeline

### Scenario 1: Backend Down

```
Time  User Action              UI State
────  ─────────────────────    ──────────────────────────────
T0    Open http://127.0.0.1    Loading...
      :5173
      
T1    App loads                Status Panel:
      (2-3 seconds)            ✗ Backend Service: Not responding
                               💡 Make sure to run: cd backend...
                               
T2    User reads hint           Status Panel visible
      and runs backend
      
T3    Backend starts            Configuration checks running...
      (30 seconds elapsed)      
      
T4    Next auto-check           Status Panel:
      (every 30s)              ✓ Backend Service: Connected
                               
T5    User clicks               ✓ Wallet connection works!
      "Connect Wallet"
```

### Scenario 2: All Good

```
Time  User Action              UI State
────  ─────────────────────    ──────────────────────────────
T0    Prerequisite:            (Backend already running)
      Backend started
      
T1    Open app                 App loads
                               
T2    Configuration checks     Status panel runs checks...
      complete (2-3s)          
      
T3    All green ✓              Status panel HIDDEN
                               (all good - no warnings)
                               
T4    User clicks               ✓ Wallet connection works!
      "Connect Wallet"         (Backend ready to handle)
```

## Component Interaction Diagram

```
┌──────────────────────────────────────────────────────────┐
│                    Home Page                             │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  ConfigurationStatus Component                     │ │
│  │  ┌──────────────────────────────────────────────┐ │ │
│  │  │ Uses: useAppConfiguration Hook               │ │ │
│  │  │ Displays: Backend & Chia Node status         │ │ │
│  │  └──────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  ChiaNodeConnector Component                       │ │
│  │  (Set RPC URL, test connection)                   │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  Hero Section & Links                             │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│         Navbar (appears on all pages)                   │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  WalletConnector Component                         │ │
│  │  ┌──────────────────────────────────────────────┐ │ │
│  │  │ Uses: useAppConfiguration Hook               │ │ │
│  │  │ Blocks: If backend not ready                 │ │ │
│  │  │ Shows: Error if connection not possible      │ │ │
│  │  └──────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## Configuration Check Results Matrix

```
┌────────────────────┬──────────────┬──────────────┬─────────────┐
│  Backend Ready     │ Chia Node OK │ Wallet Ready │ Appearance  │
├────────────────────┼──────────────┼──────────────┼─────────────┤
│ ✓ Yes              │ ✓ Yes        │ ✓ Yes        │ Hidden      │
│                    │              │              │ (all good)  │
├────────────────────┼──────────────┼──────────────┼─────────────┤
│ ✗ No               │ n/a          │ ✗ No         │ Red error   │
│                    │              │              │ + hint      │
├────────────────────┼──────────────┼──────────────┼─────────────┤
│ ✓ Yes              │ ✗ No         │ ✓ Yes        │ Yellow warn │
│                    │              │              │ (optional)  │
├────────────────────┼──────────────┼──────────────┼─────────────┤
│ ✓ Yes              │ ⏳ Checking  │ ✓ Yes        │ Gray info   │
│                    │              │              │ (loading)   │
└────────────────────┴──────────────┴──────────────┴─────────────┘
```

## Auto-Check Timing

```
App Load
   │
   ▼
Initial Check (immediate)
   │
   ├─ 2-3 seconds to complete
   │
   ▼
Recurring Check Timer Started
   │
   ├─ Every 30 seconds
   │
   ├─ Check 1: 30s
   ├─ Check 2: 60s
   ├─ Check 3: 90s
   └─ ... continues
   
User Closes App or Leaves Page
   │
   ▼
Timer Cleared (cleanup)
```

## Error Recovery Flow

```
User Sees Error:
"Backend service is not running..."
   │
   ▼
User Runs:
cd backend && cargo run
   │
   ▼
Backend Starts
(takes 3-5 seconds)
   │
   ▼
Backend Ready
(logs: "Server listening on...")
   │
   ▼
User clicks "Connect Wallet" again
(no manual refresh needed!)
   │
   ▼
Config checks run automatically
(next 30s interval OR triggered)
   │
   ▼
Status updates to: ✓ Backend Ready
   │
   ▼
✓ Wallet Connection Succeeds!
```

## Service Dependencies

```
Application
    │
    ├─ Frontend (React/Vite)
    │  └─ Requires: Backend to be running
    │
    └─ Backend (Rust/Axum)
       ├─ Requires: PostgreSQL database
       └─ Optional: Chia node RPC endpoint
```

## Health Check Endpoints

```
┌─────────────────────────────────────────┐
│  Frontend (Port 5173)                   │
│  ├─ /                (App root)         │
│  └─ Makes calls to:                     │
│                                         │
│     Backend (Port 8080)                 │
│     ├─ /health              (Status)    │
│     ├─ /chia/node/status   (Blockchain)│
│     └─ /api/rpc             (Main API) │
│                                         │
│     Database (Port 5432)                │
│     └─ (via backend only)               │
│                                         │
│     Chia Node (Port 8555)               │
│     └─ (optional, via backend)          │
└─────────────────────────────────────────┘
```
